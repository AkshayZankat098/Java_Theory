package Selenium_Theory;

public class SeleniumLocator {

// ID, Name, ClassName, TagName
// LinkText: Selenium locator strategy used to locate hyperlinks (<a> tags) based on the exact visible text of the link. It only works with elements that use the HTML <a> tag.
// Partial LinkText: Instead of full text only required the Partial text from the full text.
// Css Selector:
// Xpath:

}
