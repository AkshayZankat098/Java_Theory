package Selenium_Theory;

public class SeleniumMethods {

// Important Selenium Methods :

// What is WebElement: In Selenium, WebElement is an interface/Data type that represents an HTML element on a web page (like input box, button, link).
// 1) findElement(): returns a single WebElement > WebElement searchBox = driver.findElement(By.name("q"));
// 2) findElements(): returns list of WebElements > List<WebElement> links = driver.findElements(By.tagName("a"));
// 3) getWindowHandle(): To get the current window handle & returns ID of current window
//                       String mainWindowHandle = driver.getWindowHandle();
// 4) getWindowHandle(): To get all open window handles (for switching between windows) & returns Set of all window handles
//                       Set<String> allWindowHandles = driver.getWindowHandles();

}
